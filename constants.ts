import { Product, Movement } from './types';

// Los datos de ejemplo se eliminan para usar la conexión con Firestore.
// La aplicación ahora leerá y escribirá datos reales en la base de datos en la nube.
export const MOCK_PRODUCTS: Product[] = [];

export const MOCK_MOVEMENTS: Movement[] = [];
